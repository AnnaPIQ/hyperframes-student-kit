# Node
node_modules/

# Caches
.cache/
.tmp-*/

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Hyperframes scratch renders (final.mp4 at each project root ships instead)
renders/

# Credentials / machine-local
.env
.env.*
!.env.example
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Local-only media stash (large raw recordings, source music)
raw-media/

# Raw asset drop zone — stage media here; don't commit the big raw/prepped files
assets/incoming/*
!assets/incoming/README.md

# Playwright browser profiles
.playwright-profile/

# Frame-extraction scratch
**/renders/frames/
**/renders/frames-*/

# Render scratch dirs (any path)
**/render-work-*/
**/work-*/

# Generated + source video beds for the EcomIQ audit-call ad. These are large
# and fully reproducible: montage-source.mp4 comes from Drive, the broll-*.mp4
# beds are built from it by video-projects/ecomiq-audit-call/scripts/build-broll.py.
# The voiceover and its transcript ARE committed — they're small and they're what
# every beat timing is anchored to.
**/assets/montage-source.mp4
**/assets/broll-916.mp4
**/assets/broll-45.mp4
