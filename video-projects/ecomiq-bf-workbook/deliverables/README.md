# Shipped pair — EcomIQ Black Friday workbook ad

The two finals, committed here rather than to `renders/` (which is gitignored)
so they have stable download URLs. Both are `--quality standard`, CRF 18,
visually lossless at 1080p, which is the delivery spec for Meta placements.

| Cut | File | Dimensions | Duration | Size |
|---|---|---|---|---|
| 9:16 Story / Reels | `ecomiq-bf-workbook-9x16.mp4` | 1080×1920 @ 30fps | 52.33s | 29.0 MB |
| 4:5 Meta feed | `ecomiq-bf-workbook-4x5.mp4` | 1080×1350 @ 30fps | 52.33s | 19.9 MB |

H.264 High, AAC audio. No subtitles are burned in — the bottom band of each
frame is reserved for them and they go on manually downstream (`../DESIGN.md`).

**These are build outputs, not sources.** Regenerate with the commands in
`../DESIGN.md` and copy them back here; never edit them in place. They are
committed for delivery convenience, so if the repo needs slimming these are the
first thing to drop.
